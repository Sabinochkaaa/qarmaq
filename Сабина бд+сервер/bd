create_statement
"CREATE TABLE IF NOT EXISTS alerts (id bigint NOT NULL, channel_id bigint, post_id bigint, wallet_id bigint, alert_type text NOT NULL, message text, risk_score real, is_read boolean, created_at timestamp with time zone);"
"CREATE TABLE IF NOT EXISTS all_data (id bigint, channel_name text, channel_tg_id bigint, channel_username text, message_id bigint, message_link text, message_date timestamp with time zone, sender_id bigint, sender_type text, raw_text text, normalized_text text, category text, category_raw text, risk_score real, explanation text, prefilter_hints ARRAY, is_suspicious boolean, analyzed_at timestamp with time zone, model text, red_flags ARRAY, wallet_ids ARRAY, wallets ARRAY, article_numbers ARRAY, article_labels ARRAY, article_links ARRAY, article_penalties ARRAY);"
"CREATE TABLE IF NOT EXISTS bot_messages (id bigint NOT NULL, chat_id bigint NOT NULL, username text, message_text text, is_from_user boolean, created_at timestamp with time zone);"
"CREATE TABLE IF NOT EXISTS bot_states (chat_id bigint NOT NULL, username text, state integer, last_message text, last_message_at timestamp with time zone, updated_at timestamp with time zone);"
"CREATE TABLE IF NOT EXISTS channel_graph (id bigint, source_channel_id bigint, source_channel_name text, source_channel_tg_id bigint, target text, link_type text, discovered_url text, found_at timestamp with time zone, source_post_risk_score real, source_post_category text);"
"CREATE TABLE IF NOT EXISTS channel_links (id bigint NOT NULL, source_channel_id bigint NOT NULL, source_post_id bigint, target text NOT NULL, link_type text NOT NULL, discovered_url text, button_text text, found_at timestamp with time zone);"
"CREATE TABLE IF NOT EXISTS channels (id bigint NOT NULL, name text NOT NULL, tg_id bigint NOT NULL, username text, first_seen timestamp with time zone, last_seen timestamp with time zone);"
"CREATE TABLE IF NOT EXISTS crypto_wallets (id bigint NOT NULL, address text NOT NULL, chain text, first_seen timestamp with time zone, last_seen timestamp with time zone, tx_count integer, total_volume_usd numeric, risk_label text);"
"CREATE TABLE IF NOT EXISTS forum_posts (id bigint NOT NULL, forum_name text, title text, author text, post_text text, url text, intent text, category text, parsed_at timestamp with time zone);"
"CREATE TABLE IF NOT EXISTS legal_articles (id bigint NOT NULL, code text NOT NULL, article_number text NOT NULL, title text NOT NULL, description text, categories ARRAY NOT NULL, max_penalty text, adilet_url text, created_at timestamp with time zone);"
"CREATE TABLE IF NOT EXISTS mentions_summary (target text, link_type text, total_mentions bigint, mentioned_by_count bigint, mentioned_by_channels ARRAY);"
"CREATE TABLE IF NOT EXISTS post_articles (id bigint NOT NULL, post_id bigint NOT NULL, article_id bigint NOT NULL);"
"CREATE TABLE IF NOT EXISTS posts (id bigint NOT NULL, channel_id bigint NOT NULL, message_id bigint NOT NULL, message_link text, message_date timestamp with time zone, sender_id bigint, sender_type text, raw_text text, normalized_text text, category text NOT NULL, category_raw text, risk_score real, explanation text, prefilter_hints ARRAY, analyzed_at timestamp with time zone, model text, is_suspicious boolean, created_at timestamp with time zone);"
"CREATE TABLE IF NOT EXISTS red_flags (id bigint NOT NULL, post_id bigint NOT NULL, flag_text text NOT NULL);"
"CREATE TABLE IF NOT EXISTS wallet_dossier (wallet_id bigint, address text, chain text, first_seen timestamp with time zone, last_seen timestamp with time zone, tx_count integer, total_volume_usd numeric, risk_label text, mention_count bigint, channel_count bigint, mentioned_in_channels ARRAY, categories_found ARRAY, max_post_risk_score real);"
"CREATE TABLE IF NOT EXISTS wallet_mentions (id bigint NOT NULL, wallet_id bigint NOT NULL, post_id bigint NOT NULL, found_at timestamp with time zone);"
